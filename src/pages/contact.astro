---
/**
 * Contact page — How to reach us.
 * PRD 007: Join, Contact & Donate Pages Redesign
 */
import BaseLayout from "../layouts/BaseLayout.astro";
import Panel from "../components/ui/Panel.astro";
import { getSiteSettings } from "../lib/settings";
import { getPageCopy } from "../lib/page-copy";

const settings = await getSiteSettings();
const contactEmail = settings.contactEmail || "info@ghostbustersva.com";

// Resolve social URLs from CMS settings, with hardcoded fallbacks
const findSocial = (platform: string) =>
  settings.socialLinks.find((s) => s.platform.toLowerCase() === platform.toLowerCase())?.url;
const facebookUrl = findSocial("facebook") || "https://www.facebook.com/Ghostbusters.Virginia.757";
const instagramUrl = findSocial("instagram") || "https://www.instagram.com/ghostbustersva/";
const tiktokUrl = findSocial("tiktok") || "https://www.tiktok.com/@ghostbustersvirginia";
const youtubeUrl = findSocial("youtube") || "https://www.youtube.com/@Ghostbusters_Virginia";

const copy = (await getPageCopy("contact")) as {
  pageTitle?: string;
  pageIntro?: string;
  bookUsText?: string;
  serviceAreasText?: string;
  regions?: { title: string; areas: string[] }[];
  ctaCallout?: string;
  ctaAside?: string;
  footnoteText?: string;
} | null;

const regions = copy?.regions ?? [
  {
    title: "Northern Virginia",
    areas: ["NOVA / DC Metro", "Fairfax & Loudoun", "Arlington & Alexandria"],
  },
  { title: "Central Virginia", areas: ["Richmond (804)", "Fredericksburg", "Charlottesville"] },
  {
    title: "Hampton Roads",
    areas: ["Virginia Beach (757)", "Norfolk & Chesapeake", "Newport News & Hampton"],
  },
];
---

<BaseLayout
  title="Contact"
  description="Contact Ghostbusters Virginia for event bookings, questions, and community collaboration opportunities."
  canonical="/contact"
  ogTitle="Contact Ghostbusters Virginia"
  ogDescription="Book Ghostbusters Virginia for events or connect with us through email and social platforms."
  ogImage="/images/ghostbusters-car.jpg"
>
  <section class="container page" aria-labelledby="contact-title">
    <h1 id="contact-title" class="page-title">{copy?.pageTitle || "Contact Us"}</h1>
    <p class="page-intro">
      {
        copy?.pageIntro ||
          "Have a question, want to book us for an event, or just want to say hi? Reach out via the info below or connect with us on social media."
      }
    </p>

    <Panel heading="Contact Info">
      <div class="social-grid">
        <a href={`mailto:${contactEmail}`} class="social-link">
          <span class="social-icon" aria-hidden="true">
            <svg
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="1.5"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <rect x="2" y="4" width="20" height="16" rx="2"></rect>
              <path d="M22 4L12 13 2 4"></path>
            </svg>
          </span>
          <span class="social-label">Email</span>
          <span class="social-handle">{contactEmail}</span>
        </a>
        <a href={facebookUrl} target="_blank" rel="noopener noreferrer" class="social-link">
          <span class="social-icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="currentColor">
              <path
                d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"
              ></path>
            </svg>
          </span>
          <span class="social-label">Facebook</span>
          <span class="social-handle">Ghostbusters Virginia</span>
        </a>
        <a href={instagramUrl} target="_blank" rel="noopener noreferrer" class="social-link">
          <span class="social-icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="currentColor">
              <path
                d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 100 12.324 6.162 6.162 0 000-12.324zM12 16a4 4 0 110-8 4 4 0 010 8zm6.406-11.845a1.44 1.44 0 100 2.881 1.44 1.44 0 000-2.881z"
              ></path>
            </svg>
          </span>
          <span class="social-label">Instagram</span>
          <span class="social-handle">@ghostbustersva</span>
        </a>
        <a href={tiktokUrl} target="_blank" rel="noopener noreferrer" class="social-link">
          <span class="social-icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="currentColor">
              <path
                d="M19.59 6.69a4.83 4.83 0 01-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 01-2.88 2.5 2.89 2.89 0 01-2.89-2.89 2.89 2.89 0 012.89-2.89c.28 0 .54.04.79.1v-3.5a6.37 6.37 0 00-.79-.05A6.34 6.34 0 003.15 15.2a6.34 6.34 0 0010.86 4.48V13a8.28 8.28 0 005.58 2.16v-3.45a4.85 4.85 0 01-3.77-1.25V6.69h3.77z"
              ></path>
            </svg>
          </span>
          <span class="social-label">TikTok</span>
          <span class="social-handle">@ghostbustersvirginia</span>
        </a>
        <a href={youtubeUrl} target="_blank" rel="noopener noreferrer" class="social-link">
          <span class="social-icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="currentColor">
              <path
                d="M23.498 6.186a3.016 3.016 0 00-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 00.502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 002.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 002.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"
              ></path>
            </svg>
          </span>
          <span class="social-label">YouTube</span>
          <span class="social-handle">@Ghostbusters_Virginia</span>
        </a>
      </div>
    </Panel>

    <Panel heading="Book Us for Your Event">
      <p class="book-us-text">
        {
          copy?.bookUsText ||
            "Do you have a company function, parade, event opening, charity event, or just looking to bring the supernatural fun to a party? Don't wait another minute and contact your Paranormal Professionals — Ghostbusters, Virginia."
        }
      </p>
      <p class="book-us-text">
        {
          copy?.serviceAreasText ||
            "Our friendly professional team is the only 501(c)(3) non-profit Ghostbusters charity group serving you in:"
        }
      </p>
      <hr class="divider divider--full" />
      <div class="region-grid">
        {
          regions.map((region) => (
            <div class="region-col">
              <h4 class="region-title">{region.title}</h4>
              <ul class="glow-list" role="list">
                {region.areas.map((area) => (
                  <li>{area}</li>
                ))}
              </ul>
            </div>
          ))
        }
      </div>
    </Panel>

    <div class="standalone-cta">
      <strong class="cta-callout">{copy?.ctaCallout || "You know who to call!"}</strong>
      <span class="cta-aside">{copy?.ctaAside || "(or, in this case, email!)"}</span>
    </div>

    <Panel>
      <p class="footnote-text">
        * {
          copy?.footnoteText ||
            "A full contact form is coming soon. For now, please email us directly or reach out on social media."
        }
      </p>
    </Panel>
  </section>
</BaseLayout>

<style>
  /* ── Social links grid ── */
  .page :global(.panel) {
    width: 100%;
  }

  .social-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(14rem, 1fr));
    gap: var(--space-md);
  }

  .social-link {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-md) var(--space-lg);
    border-radius: var(--radius-md);
    background: var(--color-surface-alt);
    border: 1px solid var(--color-border);
    text-decoration: none;
    transition:
      border-color var(--transition-fast),
      box-shadow var(--transition-fast),
      background var(--transition-fast);
  }

  .social-link:hover {
    border-color: var(--color-hover-border);
    box-shadow: var(--shadow-hover);
    background: var(--color-chip-bg);
  }

  .social-icon {
    flex-shrink: 0;
    width: 1.5rem;
    height: 1.5rem;
    color: var(--color-accent-atmospheric);
    transition: color var(--transition-fast);
  }

  .social-icon svg {
    width: 100%;
    height: 100%;
  }

  .social-link:hover .social-icon {
    color: var(--color-link-hover);
  }

  .social-label {
    font-family: var(--font-heading);
    font-size: var(--text-xs);
    font-weight: var(--weight-semibold);
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
    color: var(--color-text);
    transition: color var(--transition-fast);
    white-space: nowrap;
  }

  .social-link:hover .social-label {
    color: var(--color-accent-hover);
  }

  .social-handle {
    display: none;
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--color-text-muted);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  @media (min-width: 640px) {
    .social-handle {
      display: block;
    }
    .social-grid {
      grid-template-columns: repeat(auto-fit, minmax(16rem, 1fr));
    }
  }

  /* ── Book Us panel ── */
  .book-us-text {
    color: var(--color-text-muted);
    font-family: var(--font-body);
    font-size: var(--text-base);
    line-height: var(--leading-normal);
    margin-bottom: var(--space-md);
  }

  .book-us-text strong {
    color: var(--color-text);
  }

  /* ── Standalone CTA callout ── */
  .standalone-cta {
    text-align: center;
    width: 100%;
  }

  .cta-callout {
    font-family: var(--font-heading);
    font-size: var(--text-2xl);
    color: var(--color-accent-atmospheric);
    letter-spacing: var(--tracking-wider);
    text-shadow: 0 0 12px var(--color-glow-atmospheric);
  }

  .cta-aside {
    display: block;
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    font-style: italic;
    margin-top: var(--space-xs);
  }

  /* ── Footnote text inside panel ── */
  .footnote-text {
    font-family: var(--font-body);
    font-size: var(--text-xs);
    color: var(--color-text-muted);
    text-align: center;
    font-style: italic;
    margin: 0;
  }

  /* ── Region grid (3 columns) ── */
  .region-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-xl);
    margin-bottom: var(--space-lg);
  }

  @media (min-width: 768px) {
    .region-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: var(--space-xl);
    }
  }

  .region-grid .region-col {
    text-align: center;
  }

  .region-title {
    font-family: var(--font-heading);
    font-size: var(--text-xs);
    font-weight: var(--weight-semibold);
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
    color: var(--color-heading-section);
    margin-bottom: var(--space-sm);
  }

  .region-grid .glow-list {
    columns: 1;
    display: inline-flex;
    flex-direction: column;
    gap: 0.375rem;
    text-align: left;
  }

  .region-grid .glow-list li {
    font-size: var(--text-sm);
    margin-bottom: 0;
  }
</style>
